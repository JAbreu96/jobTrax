# Environment & secrets
.env
.env.*
# ...but not the template. `.env.*` above was silently swallowing it, which is
# why the file the README told you to copy never existed in the repo.
!.env.example
*.json.key
*service-account*.json

# Your filled-in identity. `config/profile.example.md` is the tracked template.
config/profile.md

# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/

# Testing
.pytest_cache/
.coverage
htmlcov/

# Claude
.claude/settings.local.json

# Local data cache (contains personal/contact info)
data/
